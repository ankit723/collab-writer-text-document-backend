{
    "version": 2,
    "builds": [
        {
            "src": "api/document.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/api/document.js"
        }
    ]
}
